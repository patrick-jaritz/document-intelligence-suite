# Supabase Configuration
# These are required for the application to work
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# NOTE: API keys for OpenAI, Anthropic, Mistral, etc. should NEVER be in the frontend
# They should only be in Supabase environment variables (for Edge Functions)
# The frontend only needs Supabase credentials to call Edge Functions

# Development Mode (optional)
VITE_DEBUG_MODE=false
